#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	char f;

	char d;

	f = 'a';
	d = 'A';
	while
		(f <= 'z') {
			putchar(f);
			f++;
		}
	while
		(d <= 'Z') {
			putchar(d);
			d++;
		}
	putchar('\n');
	return (0);
}
