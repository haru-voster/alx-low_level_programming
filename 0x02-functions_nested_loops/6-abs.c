#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 * @s the interger to be computed
 * Return: The absolute value
 */

int _abs(int s)
{
	if (s >= 0)
		return (s);
	else
		return (-s);
}
